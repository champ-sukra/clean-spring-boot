spring.application.name=clean-spring-boot

# MySQL Datasource Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/promotion?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=admin123

# Hibernate/JPA Configuration (if using JPA/Hibernate)
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

